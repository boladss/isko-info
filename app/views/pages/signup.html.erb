<!DOCTYPE html>
<html>
  <head>
    <link href="signup.css" rel="stylesheet" type="text/css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap&effect=fire" rel="stylesheet">
  </head>
    <body>
    <div class="signup-as-student-two">
      <div class="group-4" id = "signup-form">

        <p class="signup-as-student">SIGNUP</p>
        <%= form_with url: signup_path, local: true, id: "signup-form-1", method: :post, onsubmit:  'return validateForm(event)' do |f|%>

          <div class="signup-name">
            <%= f.text_field :username, type: "text", placeholder: "Name", class: "fullname", id: "username"  %>
          </div>

          <div class="signup-email">
            <%= f.email_field :email, type: "email", placeholder: "Email", class: "email", id: "email" %>
          </div>

          <div class="signup-password">
            <%= f.password_field :password, id: "password", type: "password", placeholder: "Password", class: "signup-password-input" %>
          </div>

          <div class="signup-retype-password">
            <%= f.password_field :password_confirmation, id: "password_confirmation", type: "password", placeholder: "Re-type Password", class: "retype-password" %>
          </div>  
          
          <p class="signup-as-student" id = "message"></p>

          <div class="login-button">
              <%= f.submit "SIGNUP", class: "login", id: "submit"%>
          </div>

        <% end %>
      </div>
    </div>

  <% if flash[:notice].present? %>
  <script>
    document.addEventListener("DOMContentLoaded", function() {
      console.log("<%= flash[:notice] %>");
    });
  </script>
  <% end %>
  </body>
</html>
